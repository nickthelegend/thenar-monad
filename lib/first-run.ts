/** Whether this browser has driven a station before. One flag, set the first
 *  time a run starts, read only to decide whether to show the three things a
 *  new operator cannot guess. */
export const STATION_SEEN = "thenar.station.seen";
